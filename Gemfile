source "https://rubygems.org"

# Mirrors the GitHub Pages build environment more closely than a bare
# jekyll install, while still letting local builds run with Bundler.
gem "github-pages", group: :jekyll_plugins

